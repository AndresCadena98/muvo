import 'package:flutter/material.dart';

class AppColors {


  // Colores principales
  static const primary = Color(0xFF1E1E1E);      // Negro elegante
  static const secondary = Color(0xFF2D2D2D);    // Negro suave
  static const accent = Color(0xFF3D3D3D);       // Gris oscuro

  // Colores de fondo
  static const background = Color(0xFF121212);   // Negro puro
  static const surface = Color(0xFF1E1E1E);      // Negro elegante
  static const surfaceVariant = Color(0xFF2D2D2D); // Negro medio

  // Colores de texto
  static const onBackground = Color(0xFFFFFFFF); // Blanco puro
  static const onSurface = Color(0xFFF5F5F5);    // Blanco suave
  static const onPrimary = Color(0xFFFFFFFF);    // Blanco puro

  // Estados
  static const error = Color(0xFFE57373);        // Rojo suave
  static const success = Color(0xFF81C784);      // Verde suave
  static const warning = Color(0xFFFFB74D);      // Naranja suave

  // Gradientes
  static const gradientStart = Color(0xFF1E1E1E);
  static const gradientEnd = Color(0xFF2D2D2D);
} 